#LIST_Q84.py
# 84. Write a Python program to round the numbers of a given list, print the minimum and maximum numbers and multiply the numbers by 5.
	 #Print the unique numbers in ascending order separated by space. 
#	Original list: [22.4, 4.0, 16.22, 9.1, 11.0, 12.22, 14.2, 5.2, 17.5]
#	Minimum value: 4
#	Maximum value: 22
#	Result:     20 25 45 55 60 70 80 90 110
import functools as fc
def opr(k):
	lst = []
	for i in k:
		rd = lst.append(round(i))
	val = fc.reduce(lambda x,y:x if x>y else y ,lst)
	print("Maximum Value = ",val)
	val = fc.reduce(lambda x,y:x if x<y else y ,lst)
	print("Minimum Value = ",val)
	lst.sort()
	for i in lst:
		res = (i*5)
	print('Result=',res,end=" ")
	print("="*50)


# Main program
org_lst = [22.4, 4.0, 16.22, 9.1, 11.0, 12.22, 14.2, 5.2, 17.5]
opr(org_lst )