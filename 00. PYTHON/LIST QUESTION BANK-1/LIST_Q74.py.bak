# LIST_Q74.py
#74. Write a Python program to pack consecutive duplicates of a given list elements into sublists.
#	Original list:	[0, 0, 1, 2, 3, 4, 4, 5, 6, 6, 6, 7, 8, 9, 4, 4]
#	After packing consecutive duplicates of the said list elements into sublists:
#	[[0, 0], [1], [2], [3], [4, 4], [5], [6, 6, 6], [7], [8], [9], [4, 4]]
def pack_consecutive_duplicates(input_list):
    result = []
    sublist = [input_list[0]]
    for element in input_list[1:]:
        print("from for loop",element)
		print()
        if element == sublist[-1]:
            sublist.append(element)
            print("if part",sublist)
            print("---------")
        else:
            result.append(sublist)
            sublist = [element]
			print()
            print("I am from else",result)
    result.append(sublist)  # Append the last sublist
    return result
# Test case
original_list = [0, 0, 1, 2, 3, 4, 4, 5, 6, 6, 6, 7, 8, 9, 4, 4]
packed_list = pack_consecutive_duplicates(original_list)
print(packed_list)  # Expected output: [[0, 0], [1], [2], [3], [4, 4], [5], [6, 6, 6], [7], [8], [9], [4, 4]]
