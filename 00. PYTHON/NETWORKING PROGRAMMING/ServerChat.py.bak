#ServerChat.py
import socket
s=socket.socket()
s.bind(("127.0.0.1",8558))
s.listen(2)
while(True):
	ca,cs=s.accept()
	csdata=cs.recv(1024).decode()
	print("Student-->{}".format(csdata))
	sdata=input("PRS-->")
	cs.send(sdata.encode())
